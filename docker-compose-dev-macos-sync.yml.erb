version: '2.3'
services:
  web:
    environment:
      APP_USER: 'build'
      APP_GROUP: 'build'
      XDEBUG_REMOTE_HOST: 'host.docker.internal'
    volumes:
      - <%= config.name %>-sync:/app:nocopy

volumes:
  <%= config.name %>-sync:
    external: true
